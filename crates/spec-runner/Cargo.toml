[package]
name = "spec-runner"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
wasm-core = { path = "../wasm-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
